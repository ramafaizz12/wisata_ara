part of 'widgets.dart';

class ContainerHomeUMKM extends StatelessWidget {
  const ContainerHomeUMKM({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("");
    // return Column(
    //   children: [
    //     Text("Wisata Popular"),
    //     GridView.builder(
    //         itemCount: 3,
    //         scrollDirection: Axis.horizontal,
    //         gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
    //             crossAxisCount: 2, childAspectRatio: (16 / 9)),
    //         itemBuilder: (context, index) => ContainerWisata(width,))
    //   ],
    // );
  }
}
